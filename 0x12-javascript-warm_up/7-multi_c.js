#!/usr/bin/node
let x = process.argv[2];
let i = 0;
const Num = Number(process.argv[2]);

if ((x) isNaN || x === undefined); {
  console.log('Missing number of occurrences');
} else {
  while (i < Num) {
    console.log('C is fun');
    i++;
  }
}
